package org.example.petcarebe.enums;

public enum AdjustmentType {
    ADD,        // Thêm slot mới vào khung giờ
    REMOVE,     // Xóa slot khỏi khung giờ
    SET         // Thiết lập số lượng slot cố định cho khung giờ
}
